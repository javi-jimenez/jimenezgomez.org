# Example env file for local crontab-custom-ia scripts
# Copy this to ~/.config/crontab-custom-ia/env and set secure permissions (chmod 600)

# GitHub repository (owner/repo)
GITHUB_REPOSITORY="javi-jimenez/jimenezgomez.org"

# Personal access token with 'repo' scope to allow creating issues (only if you want this)
# GITHUB_TOKEN="ghp_xxx"

# Planner settings
SCHEDULE_DAYS=7
TOP_N=5
PLANNER_AUTHOR="Francisco Javier"
PLANNER_LOCATION="Valencia, España"

# Optional: LinkedIn publish credentials (for local scripts). Do NOT commit secrets.
# Create ~/.config/linkedin_publish.env with these values (chmod 600) or set them in your
# local environment before running social publish scripts.
# LINKEDIN_ACCESS_TOKEN="your_long_lived_token"
# LINKEDIN_URN="urn:li:person:XXXXXXXX"
